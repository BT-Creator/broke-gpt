import { Center, Title } from "@mantine/core";

export default function Header(){
    return (
        <Center w="100vw" h="100%" bg="#171717">
          <Title order={1} c="white">Broke GPT</Title>
        </Center>
    );
}